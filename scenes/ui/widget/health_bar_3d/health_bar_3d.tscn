[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scenes/ui/widget/health_bar_3d/health_bar_3d.gd" id="1"]

; Intentionally a single node. The four visual layers (Track / Fill / Rim / Value)
; are INTERNAL children the script builds at runtime, so Godot never serialises
; their generated textures as instance overrides into the five tower scenes.
; Everything is tuned through the exported knobs on this root node.

[node name="HealthBar3D" type="Node3D"]
script = ExtResource("1")
