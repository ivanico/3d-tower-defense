[plugin]

name="UI Icon Import Cap"
description="Forces process/size_limit on every UI icon, so replacing an image file cannot silently reintroduce a full-size texture."
author="Tower's Last Stand"
version="1.0"
script="plugin.gd"
