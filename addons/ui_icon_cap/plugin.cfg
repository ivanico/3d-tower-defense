[plugin]

name="UI Icon Import Cap"
description="Forces a size-appropriate process/size_limit and mipmaps/generate=true on every UI image (icons, buttons, panels, cards, backgrounds), so replacing an image file cannot silently reintroduce a full-size, non-mipmapped texture."
author="Tower's Last Stand"
version="1.0"
script="plugin.gd"
